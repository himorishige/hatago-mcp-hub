{
  "version": 1,
  "logLevel": "debug",
  "mcpServers": {
    "test-server": {
      "type": "local",
      "command": "echo",
      "args": ["Test MCP Server"],
      "activationPolicy": "onDemand",
      "idlePolicy": {
        "idleTimeoutMs": 10000,
        "minLingerMs": 5000
      }
    },
    "always-server": {
      "type": "local",
      "command": "echo",
      "args": ["Always Running Server"],
      "activationPolicy": "always"
    }
  }
}
