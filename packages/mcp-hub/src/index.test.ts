import { describe, it, expect } from 'vitest';

describe('mcp-hub package', () => {
  it('should have placeholder test', () => {
    // Placeholder test to prevent vitest from failing
    // This package is a meta-package that re-exports from other packages
    expect(true).toBe(true);
  });
});
