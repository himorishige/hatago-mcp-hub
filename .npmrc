# Publish public packages by default
access=public

# Avoid generating package-lock.json in a pnpm workspace
package-lock=false

# Enforce declared engine constraints
engine-strict=true

# Workspace awareness (npm >=8)
prefer-workspace-packages=true
# workspace=false  # Commented out - causes "filters.reduce is not a function" error with npm 10.9.3

