<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Hatago MCP Hub - Redirecting...</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script>
    // Browser language detection and redirect for static hosting
    (function() {
      const defaultLocale = 'ja';
      const supportedLocales = ['ja', 'en'];
      
      // Get browser language
      const browserLang = (navigator.language || navigator.userLanguage || '').split('-')[0].toLowerCase();
      
      // Determine target locale
      let targetLocale = defaultLocale;
      if (browserLang && supportedLocales.includes(browserLang)) {
        targetLocale = browserLang;
      }
      
      // Redirect to localized homepage
      window.location.replace('/' + targetLocale + '/');
    })();
  </script>
  <noscript>
    <meta http-equiv="refresh" content="0; url=/ja/">
  </noscript>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }
    .container {
      text-align: center;
      padding: 2rem;
    }
    .logo {
      font-size: 3rem;
      margin-bottom: 1rem;
    }
    .links {
      margin-top: 2rem;
    }
    .links a {
      color: white;
      text-decoration: none;
      padding: 0.5rem 1rem;
      margin: 0 0.5rem;
      border: 2px solid white;
      border-radius: 0.25rem;
      display: inline-block;
      transition: all 0.3s;
    }
    .links a:hover {
      background: white;
      color: #764ba2;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">🏮</div>
    <h1>Hatago MCP Hub</h1>
    <p>Redirecting to your language preference...</p>
    <div class="links">
      <a href="/ja/">日本語</a>
      <a href="/en/">English</a>
    </div>
  </div>
</body>
</html>